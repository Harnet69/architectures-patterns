<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.archpattern">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArchPattern">
        <activity
            android:name=".MVIActivity.MVIActivity"
            android:exported="true"
            android:label="@string/pattern_mvi" />
        <activity
            android:name=".mvvm.MVVMActivity"
            android:exported="true"
            android:label="@string/pattern_mvvm" />
        <activity
            android:name=".mvp.MVPActivity"
            android:exported="true"
            android:label="@string/pattern_mvp" />
        <activity
            android:name=".mvc.MVCActivity"
            android:exported="true"
            android:label="@string/pattern_mvc" />
        <activity
            android:name=".ArchActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>